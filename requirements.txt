# Core (required)
pymavlink>=2.4.40
pyserial>=3.5

# GCS UI — PyQt6 + QML + Leaflet map
# QML support is included in the base PyQt6 package (no separate PyQt6-QML needed)
PyQt6>=6.6
PyQt6-WebEngine>=6.6

# Legacy QWidget UI (--legacy flag only, optional)
# pyqtgraph>=0.13

# Development
pytest>=7.0
